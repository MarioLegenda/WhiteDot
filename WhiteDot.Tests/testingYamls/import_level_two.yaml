select:
  find_single:
    sql: "SELECT * FROM employees.employee WHERE id = @id"
    properties:
      - first_name:FirstName
      - last_name:LastName
      - id:Id
      - birth_date:BirthDate
      - hire_date:HireDate