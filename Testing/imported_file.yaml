select:
  find_first_user:
    sql: "SELECT * FROM employees.employee LIMIT 1"
    properties:
      - first_name:FirstName
      - last_name:LastName
      - id:Id
      - birth_date:BirthDate
      - hire_date:HireDate